<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".MainActivity">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/frChatMessageText"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:hint="@string/edit_text_message_typehere"
        android:layout_toStartOf="@+id/frChatSendButton" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/button_send"
        android:id="@+id/frChatSendButton"
        android:layout_alignTop="@+id/frChatMessageText"
        android:layout_alignParentEnd="true" />

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/frChatMessageList"
        android:layout_alignParentStart="true"
        android:layout_above="@+id/frChatMessageText"
        android:transcriptMode="alwaysScroll"
        android:stackFromBottom="true"/>

</RelativeLayout>
